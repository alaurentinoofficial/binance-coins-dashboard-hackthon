import { KafkaConfig, ProducerConfig } from 'kafkajs';
import { Subject } from 'rxjs';
declare type JSONObject = {
    [key: string]: JSON;
};
declare type JSONArray = Array<JSON>;
declare type JSON = null | string | number | boolean | JSONArray | JSONObject;
declare const rxkfk: (options: KafkaConfig, topic: string, producerOptions?: ProducerConfig | undefined) => {
    pushMessage$$: Subject<JSON | undefined>;
};
export default rxkfk;
