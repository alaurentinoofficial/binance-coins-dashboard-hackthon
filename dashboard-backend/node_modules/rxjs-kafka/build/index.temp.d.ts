import { ConsumerConfig, KafkaConfig, ProducerConfig } from 'kafkajs';
import { Observable, Observer } from 'rxjs';
declare type JSONObject = {
    [key: string]: JSON;
};
declare type JSONArray = Array<JSON>;
declare type JSON = null | string | number | boolean | JSONArray | JSONObject;
declare const fromKafkaTopic: (options: KafkaConfig, topic: string /** Can't be `ConsumerSubscribeTopic` because producer needs string */, consumerOptions?: ConsumerConfig | undefined, producerOptions?: ProducerConfig | undefined) => {
    message$$: Observable<JSON | undefined>;
    pushMessage: Observer<JSON | undefined>;
};
export { fromKafkaTopic };
