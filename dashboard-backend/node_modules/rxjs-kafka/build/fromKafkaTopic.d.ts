import { ConsumerConfig, ConsumerSubscribeTopic, KafkaConfig } from 'kafkajs';
import { Observable } from 'rxjs';
declare type JSONObject = {
    [key: string]: JSON;
};
declare type JSONArray = Array<JSON>;
declare type JSON = null | string | number | boolean | JSONArray | JSONObject;
declare const rxkfk: (kafkaOptions: KafkaConfig, topicOptions: ConsumerSubscribeTopic | string, consumerOptions?: ConsumerConfig | undefined) => Observable<JSON | undefined>;
export default rxkfk;
