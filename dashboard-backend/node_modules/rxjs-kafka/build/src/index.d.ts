import { ConsumerConfig, ConsumerSubscribeTopic, KafkaConfig, ProducerConfig } from 'kafkajs';
import { Observable, Subject } from 'rxjs';
declare type JSONObject = {
    [key: string]: JSON;
};
declare type JSONArray = Array<JSON>;
declare type JSON = null | string | number | boolean | JSONArray | JSONObject;
declare const rxkfk: (kafkaOptions: KafkaConfig, topicOptions: ConsumerSubscribeTopic | string, consumerOptions?: ConsumerConfig | undefined, producerOptions?: ProducerConfig | undefined) => {
    message$$: Observable<JSON | undefined>;
    pushMessage$$: Subject<JSON | undefined>;
};
export default rxkfk;
